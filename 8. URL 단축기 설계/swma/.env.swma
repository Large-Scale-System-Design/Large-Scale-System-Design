# App
APP_PORT=8080
BASE_URL=http://localhost:8080
NODE_ID=1

# Go module download settings for the Docker build.
# If proxy.golang.org / sum.golang.org are blocked on your network,
# keep these defaults.
GOPROXY=direct
GOSUMDB=off

# DB
DB_HOST=mariadb
DB_PORT=13306
DB_NAME=shortener
DB_USER=shortener
DB_PASS=shortenerpass
DB_ROOT_PASS=rootpass
